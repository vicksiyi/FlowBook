<swiper autoplay="{{autoplay}}" indicatorDots="{{indicatorDots}}" interval="{{interval}}" duration="{{duration}}">
  <block wx:for="{{images}}" wx:key="*this">
    <swiper-item>
      <image src="{{item}}"></image>
    </swiper-item>
  </block>
</swiper>